# Інструкція з завантаження логотипів брендів

## Опис

Цей парсер автоматично завантажує логотипи всіх брендів без використання платних API. Він використовує веб-скрейпінг та безкоштовні сервіси.

## Переваги безкоштовного рішення

✅ **Без API ключів** - не потрібні платні підписки
✅ **Автоматичний пошук** - знаходить домени через DuckDuckGo
✅ **Множинні джерела** - використовує 5+ різних джерел для логотипів
✅ **Парсинг сайтів** - аналізує офіційні сайти компаній
✅ **Fallback механізми** - якщо одне джерело не працює, пробує інші

## Джерела логотипів

1. **Website Scraping** - парсинг офіційних сайтів компаній
2. **Clearbit Logo API** - `https://logo.clearbit.com/{domain}`
3. **Logo.dev** - `https://img.logo.dev/{domain}`
4. **Google S2 Favicons** - `https://www.google.com/s2/favicons?domain={domain}&sz=256`
5. **Favicon** - прямий favicon з сайту компанії

## Файли проєкту

```
scripts/
├── brands-list.json           # Список всіх брендів за категоріями
├── download-logos-free.ts     # Головний скрипт для завантаження
└── README-LOGOS.md           # Ця інструкція

public/
└── logos/                    # Завантажені логотипи зберігаються тут

src/lib/
└── brandLogos.ts            # Автоматично згенерована мапа логотипів
```

## Встановлення залежностей

Всі необхідні залежності вже встановлені:

```bash
npm install
```

Основні пакети:
- `axios` - для HTTP запитів
- `cheerio` - для парсингу HTML
- `tsx` - для запуску TypeScript

## Використання

### 1. Запуск скрипта

```bash
npm run download-logos-free
```

### 2. Що відбувається під час виконання:

1. Скрипт читає список брендів з `scripts/brands-list.json`
2. Для кожного бренду:
   - Перевіряє чи логотип вже завантажений
   - Якщо ні, шукає домен через DuckDuckGo
   - Намагається завантажити логотип з різних джерел
   - Зберігає в `public/logos/`
3. Генерує файл `src/lib/brandLogos.ts` з мапою всіх логотипів

### 3. Статистика виконання

Після завершення ви побачите:

```
📊 STATISTICS
==================================================
Total brands:      250
Downloaded:        215 ✅
Reused existing:   30 ♻️
Failed:            5 ❌
Success rate:      98.0%
==================================================
```

## Додавання нових брендів

Відредагуйте файл `scripts/brands-list.json`:

```json
{
  "categories": {
    "USA": [
      "Новий бренд 1",
      "Новий бренд 2"
    ],
    "EUROPE": [
      "Європейський бренд"
    ]
  }
}
```

Потім запустіть скрипт знову:

```bash
npm run download-logos-free
```

## Налаштування

У файлі `download-logos-free.ts` можна змінити:

```typescript
const DELAY_MS = 1000;  // Затримка між запитами (мс)
const MAX_RETRIES = 3;  // Кількість спроб при помилці
```

## Структура згенерованого файлу

Файл `src/lib/brandLogos.ts` матиме таку структуру:

```typescript
export const BRAND_LOGO_MAP: Record<string, string> = {
  '1221 wheels': '/logos/1221-wheels.png',
  'ABT': '/logos/abt.svg',
  'Akrapovic': '/logos/akrapovic.png',
  // ... інші бренди
};

export const getBrandLogo = (brandName: string): string => {
  return BRAND_LOGO_MAP[brandName] || '/logos/placeholder.svg';
};
```

## Використання в коді

```typescript
import { getBrandLogo } from '@/lib/brandLogos';

// Отримати шлях до логотипу
const logoPath = getBrandLogo('ABT');

// В React компоненті
<Image
  src={getBrandLogo('Akrapovic')}
  alt="Akrapovic Logo"
  width={200}
  height={100}
/>
```

## Підтримувані формати

- `.svg` (найвищий пріоритет)
- `.png`
- `.webp`
- `.jpg` / `.jpeg`
- `.ico`

## Troubleshooting

### Проблема: DuckDuckGo блокує запити

**Рішення**: Збільште затримку між запитами:

```typescript
const DELAY_MS = 2000; // 2 секунди
```

### Проблема: Не може знайти домен

**Рішення**: Додайте домен вручну в `brands-list.json`:

```json
{
  "name": "Назва бренду",
  "domain": "example.com"
}
```

### Проблема: Логотип низької якості

**Рішення**: Скачайте вручну якісний логотип та помістіть в `public/logos/` з правильним ім'ям (slug).

## Обмеження

⚠️ **Rate Limiting** - DuckDuckGo може тимчасово блокувати при великій кількості запитів
⚠️ **Якість** - деякі логотипи можуть бути низької якості (favicon замість SVG)
⚠️ **Час виконання** - для ~250 брендів може зайняти 5-10 хвилин

## Порівняння з платним API (Brandfetch)

| Характеристика | Безкоштовне рішення | Brandfetch API |
|----------------|---------------------|----------------|
| Вартість | ✅ Безкоштовно | ❌ $50+/місяць |
| API ключ | ✅ Не потрібен | ❌ Потрібен |
| Якість логотипів | ⚠️ Варіюється | ✅ Висока |
| Швидкість | ⚠️ Повільніше | ✅ Швидко |
| Rate limits | ⚠️ Можливі обмеження | ✅ Стабільні ліміти |

## Ліцензія

Використовуйте логотипи відповідно до авторських прав їх власників.

---

**Створено**: 2025
**Автор**: OneCompany Team
