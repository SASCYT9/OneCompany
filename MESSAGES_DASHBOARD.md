# Messages Dashboard - Web3 Storage

## 🎯 Що це?

Повноцінна панель для перегляду та управління всіма повідомленнями з:
- **Telegram бота** (команди та прямі повідомлення)
- **Контакт-форми** (авто та мото запити)

Всі повідомлення зберігаються в **in-memory store** (Web3 архітектура) з можливістю експорту/імпорту.

---

## ✨ Функціонал

### 📊 Дашборд
- **Статистика в реальному часі**
  - Загальна кількість повідомлень
  - Нові, прочитані, відповіді, архів
  - По категоріям: авто, мото, загальні
  - Повідомлення за сьогодні

### 🔍 Фільтрація та Пошук
- Фільтр по статусу (нові, прочитані, відповіді, архів)
- Фільтр по категорії (авто, мото, загальні)
- Текстовий пошук по повідомленням та іменам

### 💬 Управління повідомленнями
- **Перегляд деталей** - повна інформація про повідомлення
- **Зміна статусу** - прочитано, відповідь, архів
- **Відповіді** - збереження історії комунікації
- **Видалення** - очищення непотрібних повідомлень

### 🔐 Безпека
- Автентифікація паролем
- API захищений Bearer токеном
- Session persistence

---

## 🚀 Швидкий старт

### 1. Доступ до дашборду

Відкрийте в браузері:
```
http://localhost:3001/messages
```

### 2. Введіть пароль

За замовчуванням: `admin123` (той самий що і для `/admin`)

Змініть в `.env.local`:
```env
ADMIN_PASSWORD=your_secure_password
```

### 3. Готово!

Ви побачите:
- 📊 Статистику повідомлень
- 📝 Список всіх повідомлень
- 🔍 Фільтри та пошук

---

## 📱 Як використовувати

### Перегляд повідомлень

1. **Список повідомлень** (ліворуч)
   - Клікніть на повідомлення щоб відкрити деталі
   - Нові повідомлення автоматично позначаються як "Прочитані"

2. **Деталі повідомлення** (праворуч)
   - Повний текст
   - Інформація про відправника
   - Час отримання
   - Тип та категорія

### Зміна статусу

Використовуйте кнопки:
- ✅ **Прочитано** - позначити як прочитане
- ✉️ **Відповідь** - після відправки відповіді
- 📦 **Архів** - заархівувати

### Відповіді

1. Введіть текст відповіді внизу
2. Натисніть "Відправити" або `Enter`
3. Відповідь збережеться в історії
4. Статус автоматично зміниться на "Відповідь"

### Видалення

Натисніть 🗑️ у правому верхньому куті деталей повідомлення

---

## 🗄️ Архітектура зберігання

### In-Memory Store (Поточна реалізація)

```typescript
// src/lib/messageStore.ts
class MessageStore {
  private messages: Map<string, TelegramMessage>
  
  addMessage()      // Додати нове
  getAllMessages()  // Отримати всі
  updateStatus()    // Змінити статус
  addReply()        // Додати відповідь
  deleteMessage()   // Видалити
  getStats()        // Статистика
  export()          // Експорт в JSON
  import()          // Імпорт з JSON
}
```

**Переваги:**
- ⚡ Швидкість - все в пам'яті
- 🚀 Простота - без БД
- 💾 Експорт/Імпорт для бекапу

**Обмеження:**
- ❌ Дані втрачаються при рестарті сервера
- ❌ Не підходить для великих обсягів

---

## 🔄 Міграція на базу даних (Production)

Для production рекомендується PostgreSQL або MongoDB:

### Option 1: PostgreSQL (Рекомендовано)

```bash
npm install @vercel/postgres
```

```typescript
// lib/db.ts
import { sql } from '@vercel/postgres';

export async function saveMessage(message: TelegramMessage) {
  return await sql`
    INSERT INTO messages (id, chat_id, user_id, user_name, message_text, timestamp, type, category, status)
    VALUES (${message.id}, ${message.chatId}, ${message.userId}, ${message.userName}, 
            ${message.messageText}, ${message.timestamp}, ${message.type}, 
            ${message.category}, ${message.status})
  `;
}

export async function getMessages() {
  const { rows } = await sql`SELECT * FROM messages ORDER BY timestamp DESC`;
  return rows;
}
```

### Option 2: MongoDB

```bash
npm install mongodb
```

```typescript
import { MongoClient } from 'mongodb';

const client = new MongoClient(process.env.MONGODB_URI!);
const db = client.db('onecompany');
const messages = db.collection('telegram_messages');

export async function saveMessage(message: TelegramMessage) {
  return await messages.insertOne(message);
}
```

### Option 3: Prisma ORM

```bash
npm install @prisma/client
npx prisma init
```

```prisma
// schema.prisma
model Message {
  id          String   @id @default(cuid())
  chatId      Int
  userId      Int
  userName    String
  messageText String
  timestamp   DateTime @default(now())
  type        String
  category    String?
  status      String   @default("new")
  replies     Reply[]
}

model Reply {
  id        String   @id @default(cuid())
  messageId String
  text      String
  timestamp DateTime @default(now())
  sentBy    String
  message   Message  @relation(fields: [messageId], references: [id])
}
```

---

## 📊 API Endpoints

### GET /api/messages
Отримати всі повідомлення

**Headers:**
```
Authorization: Bearer your_password
```

**Query params:**
- `?status=new` - фільтр по статусу
- `?category=auto` - фільтр по категорії
- `?stats=true` - отримати статистику

**Response:**
```json
[
  {
    "id": "msg_1234567890_abc",
    "chatId": 123456,
    "userId": 789012,
    "userName": "John Doe",
    "messageText": "Потрібні запчастини для BMW M3",
    "timestamp": "2025-11-13T10:30:00Z",
    "type": "incoming",
    "category": "auto",
    "status": "new",
    "replies": []
  }
]
```

### POST /api/messages
Оновити статус або додати відповідь

**Update Status:**
```json
{
  "action": "updateStatus",
  "messageId": "msg_1234567890_abc",
  "status": "read"
}
```

**Add Reply:**
```json
{
  "action": "addReply",
  "messageId": "msg_1234567890_abc",
  "reply": "Дякуємо за запит! Зв'яжемось найближчим часом."
}
```

### DELETE /api/messages?id=xxx
Видалити повідомлення

---

## 🔒 Безпека

### Поточна реалізація
- ✅ Password authentication
- ✅ Session storage
- ✅ Bearer token API

### Рекомендації для Production
1. **JWT токени** замість простого паролю
2. **Rate limiting** для API endpoints
3. **HTTPS only** в production
4. **RBAC** - різні ролі (admin, manager, viewer)
5. **Audit log** - логування всіх дій

---

## 📦 Експорт/Імпорт даних

### Експорт (Бекап)

```typescript
const data = messageStore.export();
// Зберегти в файл або хмару
```

### Імпорт (Відновлення)

```typescript
const data = readFile('backup.json');
messageStore.import(data);
```

**Автоматичний бекап:**
```typescript
// Кожні 6 годин
setInterval(() => {
  const backup = messageStore.export();
  fs.writeFileSync(`backup-${Date.now()}.json`, backup);
}, 6 * 60 * 60 * 1000);
```

---

## 🎨 Кастомізація

### Зміна кольорів

Файл: `src/app/messages/page.tsx`

```typescript
// Статуси
const config = {
  new: { label: 'Нове', color: 'bg-blue-100 text-blue-600' },
  read: { label: 'Прочитано', color: 'bg-zinc-100 text-zinc-600' },
  // ...
};
```

### Додавання нових фільтрів

```typescript
<select value={filterType} onChange={...}>
  <option value="all">Всі типи</option>
  <option value="command">Команди</option>
  <option value="incoming">Повідомлення</option>
  <option value="contact_form">Форма</option>
</select>
```

---

## 🐛 Вирішення проблем

**Повідомлення не з'являються:**
- Перевірте чи працює Telegram webhook
- Перевірте чи контакт-форма надсилає запити
- Натисніть 🔄 для оновлення

**Не можу увійти:**
- Перевірте пароль в `.env.local`
- За замовчуванням: `admin123`
- Очистіть sessionStorage в браузері

**Дані втрачаються при рестарті:**
- Це нормально для in-memory store
- Використовуйте експорт/імпорт для бекапу
- Або мігруйте на БД (див. вище)

---

## 🚀 Наступні покращення

1. **Real-time updates** - WebSocket для live оновлень
2. **Push notifications** - сповіщення про нові повідомлення
3. **Теги та категорії** - власні мітки
4. **Пошук по датам** - фільтр по періодах
5. **Експорт в Excel/CSV** - звіти
6. **Шаблони відповідей** - швидкі відповіді
7. **Автоматичні відповіді** - AI integration
8. **Analytics** - графіки та аналітика

---

## 📚 Пов'язані файли

- `src/lib/messageStore.ts` - Головне сховище
- `src/app/api/messages/route.ts` - API endpoints
- `src/app/api/telegram/webhook/route.ts` - Telegram integration
- `src/app/api/contact/route.ts` - Contact form integration
- `src/app/messages/page.tsx` - UI дашборду

---

**Готово до використання!** 🎉

Відкрийте `/messages` та почніть керувати всіма повідомленнями в одному місці.
