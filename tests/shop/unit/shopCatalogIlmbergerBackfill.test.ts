import assert from "node:assert/strict"; import { readFileSync } from "node:fs"; import test from "node:test";
test("Ilmberger uses product-level shared persistence and guarded CLI", () => { const adapter = readFileSync("src/lib/shopCatalogIlmbergerCompatibility.server.ts", "utf8"), cli = readFileSync("scripts/backfill-catalog-v2-ilmberger.ts", "utf8"); assert.match(adapter, /variantId: null/); assert.match(adapter, /scope: "moto"/); assert.match(adapter, /persistVehicleCompatibilityInTransaction/); assert.match(cli, /option\("after"\)/); assert.match(cli, /--limit must be between 1 and 50/); assert.match(cli, /disabled in production/); });
