import assert from "node:assert/strict"; import { readFileSync } from "node:fs"; import test from "node:test";
test("all-source gate is disposable, exhaustive, parity checked, and replayed", () => { const gate = readFileSync("scripts/verify-catalog-v2-all-source-backfill.ts", "utf8"), runner = readFileSync("scripts/run-catalog-v2-all-source-backfill-docker.ts", "utf8"); assert.match(gate, /application_name=catalog-all-source-gate/); assert.match(gate, /eventuri.length \+ remus.length !== products.length/); assert.match(gate, /counts\.provenance !== expectedProvenance/); assert.match(gate, /counts\.activePolicies !== expectedRecords/); assert.match(gate, /result\.idempotent !== page\.length/); assert.match(gate, /buildShopCatalogBaselineProductEntry/); assert.match(gate, /actual\.hashes\.full !== expected\.hashes\.full/); assert.match(gate, /UA\/EN localization coverage failed/); assert.match(gate, /losslessCommerceSnapshots/); assert.match(runner, /pgvector\/pgvector:0\.8\.2-pg17/); assert.match(runner, /migrate", "deploy/); assert.match(runner, /finally/); });
test("taxonomy aliases include scope and resolved targets so auto and moto names cannot collide", () => { const persistence = readFileSync("src/lib/shopCatalogVehicleCompatibilityPersistence.server.ts", "utf8"); assert.match(persistence, /scope: input\.scope/); assert.match(persistence, /persisted\.scope !== input\.scope/); assert.match(persistence, /input\.makeId \?\? ""/); assert.match(persistence, /input\.generationId \?\? ""/); assert.match(persistence, /mixed-scope suppliers never collapse/); });
test("taxonomy alias keys use the same case-insensitive form as the database uniqueness contract", () => { const persistence = readFileSync("src/lib/shopCatalogVehicleCompatibilityPersistence.server.ts", "utf8"); assert.match(persistence, /const normalizedAlias = input\.value\.trim\(\)\.toLowerCase\(\)/); assert.match(persistence, /parentModelId \?\? ""\}\|\$\{normalizedAlias\}/); });
test("RaceChip shares canonical case-insensitive taxonomy and scoped aliases", () => { const persistence = readFileSync("src/lib/shopCatalogRaceChipCompatibility.server.ts", "utf8"); assert.match(persistence, /const normalizedMake = normalization\.make\.trim\(\)\.toLowerCase\(\)/); assert.match(persistence, /make\.normalizedName !== normalizedMake/); assert.match(persistence, /scope: "auto"/); assert.match(persistence, /persisted\.normalizedAlias !== normalizedAlias/); });
test("compatibility clause identity includes year and fuel dimensions", () => { const persistence = readFileSync("src/lib/shopCatalogVehicleCompatibilityPersistence.server.ts", "utf8"); assert.match(persistence, /application\.yearFrom \?\? "\*"/); assert.match(persistence, /application\.yearTo \?\? "\*"/); assert.match(persistence, /application\.fuel \?\? "\*"/); });
test("source backfill transactions have a bounded scale budget and retry expired serializable work", () => { const source = readFileSync("src/lib/shopCatalogSourceBackfill.server.ts", "utf8"); assert.match(source, /maxWait: 10_000/); assert.match(source, /timeout: 120_000/); assert.match(source, /error\.code === "P2028"/); assert.match(source, /attempt >= 5/); });
test("immutable replay validates evidence but skips redundant compatibility persistence", () => { const source = readFileSync("src/lib/shopCatalogSourceBackfill.server.ts", "utf8"), replay = source.slice(source.indexOf("if (existing)"), source.indexOf("const previousRecord")); assert.match(replay, /immutable replay conflict/); assert.match(replay, /idempotent \+= 1/); assert.doesNotMatch(replay, /persistCompatibility/); });
