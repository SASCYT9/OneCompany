import assert from "node:assert/strict"; import { readFileSync } from "node:fs"; import test from "node:test";
test("Remus uses product-level shared persistence and guarded subset CLI", () => { const adapter = readFileSync("src/lib/shopCatalogRemusCompatibility.server.ts", "utf8"), cli = readFileSync("scripts/backfill-catalog-v2-remus.ts", "utf8"); assert.match(adapter, /variantId: null/); assert.match(adapter, /opfGpfRelevant/); assert.match(adapter, /persistVehicleCompatibilityInTransaction/); assert.match(cli, /products.length !== 3849/); assert.match(cli, /option\("after"\)/); assert.match(cli, /--limit must be between 1 and 50/); assert.match(cli, /disabled in production/); });
