import assert from "node:assert/strict"; import test from "node:test"; import { buildRemusSourceRecordDraft, normalizeRemusSnapshotProduct, type RemusSnapshotProduct } from "../../../src/lib/shopCatalogRemusNormalization";
function product(title: string, tags: string[]): RemusSnapshotProduct { return { id: "remus-product", slug: "remus-product", sku: "REM-1", scope: "auto", brand: "Remus", title: { ua: title, en: title }, tags, variants: [] }; }
test("Remus maps make/model/year and OPF evidence", () => { const result = normalizeRemusSnapshotProduct(product("GPF-Back Exhaust for VW Golf", ["fits-make:vw", "fits-model:vw:golf-7", "fits-year:2018"])); assert.deepEqual(result.applications, [{ make: "Volkswagen", model: "Golf 7", generation: null, yearFrom: 2018, yearTo: 2018, opfGpf: "OPF/GPF" }]); assert.equal(result.opfGpfRelevant, true); });
test("Remus quarantines missing fitment", () => { const result = normalizeRemusSnapshotProduct(product("Exhaust for T-Roc", [])); assert.equal(result.mode, "NEEDS_REVIEW"); });
test("Remus recognizes explicit universal rows", () => { const result = normalizeRemusSnapshotProduct(product("Universal exhaust tip", ["fits-make:universal"])); assert.equal(result.mode, "UNIVERSAL"); assert.equal(result.engineRelevant, false); });
test("Remus keeps multi-make models correlated by namespaced tags", () => { const result = normalizeRemusSnapshotProduct(product("Exhaust for shared platform", ["fits-make:vw", "fits-model:vw:golf-8", "fits-make:audi", "fits-model:audi:a3-8y", "fits-year:2021"])); assert.deepEqual(result.applications.map((entry) => `${entry.make}:${entry.model}`), ["Volkswagen:Golf 8", "Audi:A3 8Y"]); });
test("Remus subset draft preserves every leaf", () => { const input = product("Exhaust for BMW M3", ["fits-make:bmw", "fits-model:bmw:m3"]), draft = buildRemusSourceRecordDraft({ product: input, sourceRevision: "v1" }); assert.ok(draft.provenance.every((entry) => entry.mappingStatus === "MAPPED" && entry.canonicalEntityType === "PRODUCT")); });
