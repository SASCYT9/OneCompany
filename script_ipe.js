const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.shopProduct.count({ where: { brand: { contains: 'iPE', mode: 'insensitive' } } }); console.log('iPE products:', count); } main().finally(() => prisma.$disconnect());
