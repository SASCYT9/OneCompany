import PremiumCatalogPage from "../stock/page";

export { generateMetadata } from "./metadata";

// The projection is the catalog data source, not a second storefront design.
// Keep the established premium catalog UI at the public /shop/catalog URL.
export default PremiumCatalogPage;
