# Environment variables for OneCompany (do NOT commit your real secrets)
# Copy this file to .env.local and fill in values.
# .env.local is git-ignored by default.

# Telegram Bot for contact submissions (routed by auto/moto)
# Create bot via @BotFather: /newbot -> obtain token
# Add bot to your chat/channel, forward any message from that chat to @userinfobot to get chat_id
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=          # Main chat ID for user messages forwarding (e.g., -1001234567890)
TELEGRAM_AUTO_CHAT_ID=     # Chat ID for auto inquiries from contact form (e.g., -1001111111111)
TELEGRAM_MOTO_CHAT_ID=     # Chat ID for moto inquiries from contact form (e.g., -1002222222222)

# Webhook protection (highly recommended): set a long random string and configure webhook with secret_token
TELEGRAM_WEBHOOK_SECRET=

# Admin API secret (used for webhook setup/admin management endpoints)
ADMIN_API_SECRET=

# Cron secret (used by /api/telegram/cron)
CRON_SECRET=

# Webhook configuration (for production deployment)
NEXT_PUBLIC_BASE_URL=      # Your domain without protocol (e.g., onecompany.global)
NEXT_PUBLIC_SITE_URL=https://onecompany.global  # Full site URL for sharing (used in Telegram /dashboard command)

# Google Search Console verification (copy just the code value from the meta tag)
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_ID=G-1CQVYW3WK7

# n8n Automation webhook integration
# When running via Docker compose, the Next.js container uses http://n8n:5678/webhook/contact-request
# For local dev outside Docker, point to http://localhost:5678/webhook/contact-request
AUTOMATION_ENDPOINT=http://localhost:5678/webhook/contact-request
AUTOMATION_API_KEY=         # Shared secret configured in n8n webhook node

# Email routing (via Resend or SMTP) for contact submissions
# For Resend: sign up at resend.com, verify domain, get API key
RESEND_API_KEY=
EMAIL_FROM=contact@onecompany.com    # Sender address (must be verified in Resend)
EMAIL_AUTO=auto@onecompany.com       # Recipient for auto inquiries
EMAIL_MOTO=moto@onecompany.com       # Recipient for moto inquiries

# Admin configuration
ADMIN_PASSWORD=                 # Password for /admin panel (set a strong one in production)
ADMIN_SESSION_SECRET=           # Random 32+ char string for signing admin session cookies

# Optional analytics (Plausible)
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=onecompany.example

# Notes:
# - Never commit .env.local to git. Use environment-specific secret stores in production (Vercel/Netlify/Azure/etc.).
# - Rotate tokens immediately if leaked. Change the value here and redeploy.
# - For Google Workspace email: configure MX records in GoDaddy DNS (see docs/google-workspace-setup.md).
# - For custom domain: point nameservers or A/CNAME records to your host (see docs/godaddy-domain-setup.md).
# - For Telegram setup details: see docs/telegram-bot-setup.md.
