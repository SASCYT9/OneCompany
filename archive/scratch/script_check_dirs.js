const fs = require('fs'); const path = require('path'); const shopDir = 'src/app/[locale]/shop'; const dirs = fs.readdirSync(shopDir).filter(f => fs.statSync(path.join(shopDir, f)).isDirectory() && !['components', 'data', 'cart', 'checkout', 'account', 'stock', 'turn14', '[slug]'].includes(f)); const results = dirs.map(d => { const collectionsPath = path.join(shopDir, d, 'collections'); return d + ' has collections: ' + fs.existsSync(collectionsPath); }); console.log(results.join('\n'));
