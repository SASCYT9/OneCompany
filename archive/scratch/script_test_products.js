const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.shopProduct.count({ where: { OR: [ { sku: { contains: 'test', mode: 'insensitive' } }, { titleEn: { contains: 'test', mode: 'insensitive' } }, { titleUa: { contains: 'тест', mode: 'insensitive' } } ] } }); console.log('Test products:', count); const testProducts = await prisma.shopProduct.findMany({ where: { OR: [ { sku: { contains: 'test', mode: 'insensitive' } }, { titleEn: { contains: 'test', mode: 'insensitive' } }, { titleUa: { contains: 'тест', mode: 'insensitive' } } ] } }); console.log(testProducts.map(p => p.id + ': ' + p.titleEn + ' | ' + p.brand).join('\n')); } main().finally(() => prisma.$disconnect());
