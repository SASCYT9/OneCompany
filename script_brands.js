const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const countIpe = await prisma.shopProduct.count({ where: { brand: { contains: 'ipe', mode: 'insensitive' } } }); const countAdro = await prisma.shopProduct.count({ where: { brand: { contains: 'adro', mode: 'insensitive' } } }); console.log('iPE products:', countIpe); console.log('Adro products:', countAdro); } main().finally(() => prisma.$disconnect());
