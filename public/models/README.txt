Place the high-resolution GLB models for each store in this directory.

Required filenames:
- kw-coilover.glb
- fi-exhaust.glb
- eventuri-intake.glb

Make sure the models are optimized for web delivery (reduced triangle count, baked textures, Draco compression if possible).
